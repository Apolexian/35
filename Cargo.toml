[package]
name = "heaven2_reconstructed"
version = "0.0.0"
edition = "2021"
publish = false
# Reverse-engineered reconstruction of Heaven 2.0 for tournament anti-cheat analysis.
# NOT the original source. See RECONSTRUCTION_NOTES.md.

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
